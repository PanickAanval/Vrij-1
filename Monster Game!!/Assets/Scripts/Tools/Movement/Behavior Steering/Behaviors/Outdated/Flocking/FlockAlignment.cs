/*
using UnityEngine;

namespace Steering
{
    public class FlockAlignment : FlockBase
    {
        public FlockAlignment(float radius)
        {
            m_radius = radius;
        }

        public Vector3 DesiredDirection()
        {
            if (m_neightborCount > 0)
            {
                //  Returning the average velocity direction of all neighbors.
                return averageTotal.normalized;
            }

            return Vector3.zero;
        }
    }
}
*/
